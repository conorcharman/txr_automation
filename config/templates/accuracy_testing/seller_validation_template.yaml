# Seller ID Validation Configuration Template
# ============================================
#
# Validates seller identification codes for standard ID incidents.
# Script: seller_id_validation.py
# Incidents: 16_19 (CONCAT), 16_21 (NIDN), 16_23 (CCPT)
#
# Usage:
#   python -m src.accuracy_testing.scripts.seller_id_validation \
#       --config config/local/accuracy_testing/seller_validation.yaml

# =============================================================================
# Configuration Mode
# =============================================================================
# Options: "single" or "batch"
# - single: Process one incident with explicit file paths
# - batch:  Process multiple incidents using filename patterns
mode: "batch"

# =============================================================================
# Testing Period
# =============================================================================
testing_period:
  # Fiscal year (e.g., FY25, FY26)
  fiscal_year: "FY26"
  
  # Quarter (e.g., Q1, Q2, Q3, Q4)
  quarter: "Q1"

# =============================================================================
# Single Mode Configuration
# =============================================================================
# Used when mode: "single"
# Specify exact file paths for one incident
single:
  # Incident code to validate
  incident_code: "16_21"
  
  paths:
    # Extract file (SQL database export) - validation input
    input_file: "data/extracts/16_21_FY26_Q1.csv"
    
    # Template file (Kaizen expected values) - validation reference
    template_file: "data/templates/FY26 Q1 16_21.csv"
    
    # Template column names for Kaizen lookup
    template_id_column: "Seller ID Code"
    template_type_column: "Type of Seller ID Code"
    
    # Output file (validation results)
    output_file: "data/validated/validated_FY26_Q1_16_21.csv"
    
    # Log output directory
    log_output: "logs"

# =============================================================================
# Batch Mode Configuration
# =============================================================================
# Used when mode: "batch"
# Process multiple incidents automatically
batch:
  # Incidents to process
  # Options:
  #   - "auto": Auto-discover all standard seller incidents using auto_incidents list
  #   - List: ["16_19", "16_21", "16_23"]
  incidents: "auto"
  
  # Incident codes for auto-discovery mode
  # Used when incidents: "auto" is specified
  auto_incidents:
    - "16_19"  # Incorrect CONCAT value
    - "16_21"  # Incorrect NIDN value
    - "16_23"  # Incorrect CCPT value
  
  paths:
    # Directory containing extract files (SQL database exports)
    extract_dir: "data/extracts/FY26/Q1"
    
    # Directory containing template files (Kaizen expected values)
    template_dir: "data/templates/FY26/Q1"
    
    # Directory for output files (validation results)
    output_dir: "data/validated/FY26/Q1"
    
    # Log output directory
    log_output: "logs"
  
  # Filename patterns - construct filenames for each incident
  # Available variables: {incident}, {fiscal_year}, {quarter}
  filename_patterns:
    # Extract file (SQL database export) - validation input
    # Example: 16_21_FY26_Q1.csv
    extract: "{incident}_{fiscal_year}_{quarter}.csv"
    
    # Template file (Kaizen expected values) - validation reference
    # Example: FY26 Q1 16_21.csv
    template: "{fiscal_year} {quarter} {incident}.csv"
    
    # Output file (validation results)
    # Example: validated_FY26_Q1_16_21.csv
    output: "validated_{fiscal_year}_{quarter}_{incident}.csv"

# =============================================================================
# Tracker Configuration
# =============================================================================
# Column names for tracker file lookups
tracker:
  # Column containing transaction references in tracker files
  transaction_ref_column: "Transaction Reference"
  
  # Column containing ID values in tracker files
  id_column: "Seller ID Code"
  
  # Column containing ID types in tracker files
  type_column: "Type of Seller ID Code"

# =============================================================================
# Options
# =============================================================================
options:
  # Dry run mode - preview validation without writing output files
  dry_run: false
  
  # Show progress bars during processing
  show_progress: true

# =============================================================================
# Processor Configuration
# =============================================================================
processor:
  # Logging level: DEBUG, INFO, WARNING, ERROR
  log_level: "INFO"
  
  # Enable verbose console output
  verbose: false

# =============================================================================
# Supported Incident Codes
# =============================================================================
# This script handles standard seller ID validation:
#   - 16_19: Incorrect CONCAT value within Seller identification code field
#   - 16_21: Incorrect NIDN value within Seller identification code field
#   - 16_23: Incorrect CCPT value within Seller identification code field
#
# Note: This script does NOT handle:
#   - 16_20: Inconsistent Seller ID (use inconsistent_seller_validation_template.yaml)
#   - 21_17: Seller Decision Maker (use ftsdm_validation_template.yaml)
