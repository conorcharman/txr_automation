# Buyer ID Validation Configuration
# ==================================

# File paths
paths:
  # Input CSV file with buyer ID data
  input_file: "data/test/buyer_id_sample.csv"
  
  # Output CSV file with validation results
  output_file: "data/test/buyer_id_output.csv"
  
  # Log output directory
  log_output: "logs"
  
  # Optional: Italian tracker CSV file for IT fiscal code validation
  # Format: Column C = Person Code, Column G = Status
  # italian_tracker: "path/to/italian_tracker.csv"
  
  # Optional: Main tracker CSV file for client data remediation
  # Format: Column J = Person Code, Column N = Status
  # main_tracker: "path/to/main_tracker.csv"
  
  # Optional: Template CSV file for Kaizen error lookup validation
  # Format: Column A = Transaction Ref, Columns V:W = Expected ID:Type
  # template_file: "path/to/template_7_37.csv"

# Processor configuration
processor:
  # Logging level: DEBUG, INFO, WARNING, ERROR
  log_level: "DEBUG"
  
  # Enable verbose console output
  verbose: true
  
  # Batch size for processing (future use)
  batch_size: 1000
