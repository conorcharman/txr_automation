# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
.venv/
venv/
ENV/
env/
.virtualenv/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Jupyter Notebooks
.ipynb_checkpoints/
*.ipynb

# Environment variables
.env
.env.local

# Logs

*.log
.log


# Configuration (local overrides)
config/environments/local.yaml
config/local*.yaml

# Configuration (folders with environment-specific settings)
# Track the folders but ignore files within them (except .gitkeep)

# Ignore all files in these directories
config/local/accuracy_testing/*.yaml
!config/local/accuracy_testing/.gitkeep

config/local/environments/*.yaml
!config/local/environments/.gitkeep

config/local/replay/*.yaml
!config/local/replay/.gitkeep

config/local/utils/*.yaml
!config/local/utils/.gitkeep

# Ignore local config files specifically (move this here or make it more specific)
config/local*.yaml
config/environments/local.yaml

# Output files (generated during processing)

data/output/
*.output.csv
*_processed.csv

# Temporary files
tmp/
temp/
*.tmp

# Benchmark temp data (large files)
benchmarks/temp_data/
benchmarks/**/*.csv

# SQLite cache databases (can be very large)
data/*.db
data/*.db-shm
data/*.db-wal

# Documentation (keep only confluence HTML suite)
documentation/*
!documentation/confluence/

# OS files
Thumbs.db
.DS_Store

# Package manager
.uv/

# Celery

celerybeat-schedule
data/celerybeat-schedule

# Machine-specific corporate proxy config (do not commit)
zscaler-ca.crt
.npmrc
pip-trusted-certs.pem
data/celerybeat-schedule

# Processing data

data/FY*

# Generated data directories
data/output